
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.mage</groupId>
        <artifactId>mage-server-plugins</artifactId>
        <version>1.4.60</version>
    </parent>

    <artifactId>mage-player-ai-kanna</artifactId>
    <packaging>jar</packaging>
    <name>Mage Player AI (Kanna, LLM tool-calling via Ollama)</name>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>mage</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- DARRELLBEST-FORK (keep on merge/rebase from upstream): swapped from
             mage-player-ai-mad to mage-player-ai-mcts. ComputerPlayerKanna extends
             ComputerPlayerMCTS (see that class's Javadoc for why: bounded rollout budget
             instead of MAD's exponential minimax), not the MAD player, so mage-player-ai-mcts
             is the dependency this module actually needs to compile against. -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>mage-player-ai-mcts</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>src</sourceDirectory>
    </build>

    <properties>
        <root.dir>${project.basedir}/../..</root.dir>
    </properties>

</project>
